let o = new_orchestrator();
let s = new_synth();
o.add_synth(s);
let seq = new_sequencer();
o.add_sequencer(seq);
seq.load_file("midi_files/major-scale-spaced-notes.mid");
seq.connect_synth(s);
o.play();
